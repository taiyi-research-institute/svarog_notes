CL15 论文：Linearly Homomorphic Encryption from DDH

Guilhem Castagnos, Fabien Laguillaumie

## 论文中的命题 1

$\mathcal{O}_\Delta$ 的每个（本原）理想 $\mathfrak{a}$ 都可以写成 $\mathfrak{a}=a\Z+\frac{-b+\sqrt{\Delta}}{2}\Z$，其中 $a$ 是正整数，$b$ 是整数，使得方程 $\Delta=b^2-4ac$ 存在整数解 $c$。该理想简记为 $(a,b)$。

下一个命题受 [CL09, 定理 2] 启发，建立了虚二次域类群的一个子群的存在性，在该子群上离散对数问题是容易的。

**命题 1**。设 $k$ 是形式为 $k=-pq$ 的基本判别式，其中 $k\equiv 1 \pmod 4$。设 $p$ 是奇素数，$q$ 是与 $p$ 互质的非负整数，使得 $q>4p$。设 $\mathfrak{t}=(p^2,p)$ 是 $\mathcal{O}_{kp^2}$ 的理想。用 $f=[\mathfrak{t}]$ 表示 $\mathfrak{t}$ 在类群 $C(\mathcal{O}_{kp^2})$ 中的类。对于 $m\in\left\{1,\dots,p-1\right\}$，设 $\mathtt{Reduce}(f^m)=(p^2,m^{-1}p)$，其中 $m^{-1}$ 是 $[-p, p]$ 中的奇整数，使得 $m^{-1}m\equiv 1\pmod p$。最后，$f$ 是 $C(\mathcal{O}_{kp^2})$ 的 $p$ 阶子群的生成元。

**证明**。我们考虑满射 $\bar\varphi_p:C(\mathcal{O}_{kp^2})\rightarrow C(\mathcal{O}_{k})$。根据 [CL09, 引理 1] 和 [Cos99, 命题 7.22 和定理 7.24]，$\bar\varphi_p$ 的核同构于 $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times/\left(\Z/p\Z\right)^\times$。

由于 $p\mid k$，群 $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times$ 同构于 $\left(\mathbb{F}_p[X]/(X^2)\right)^\times$。该群包含 $p(p-1)$ 个形式为 $a+b\sqrt{k}$ 的元素，其中 $a\in\left(\Z/p\Z\right)^\times$，$b\in\left(\Z/p\Z\right)$。

现在考虑商群 $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times/\left(\Z/p\Z\right)^\times$，其中 $[x]=[y]$（$x,y\in\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times$）当且仅当存在 $\lambda\in\left(\Z/p\Z\right)^\times$ 使得 $x=\lambda y$。该商是 $p$ 阶循环群，一个代表系是 $[1]$ 和 $[a+\sqrt{k}]$，其中 $a\in\left(\Z/p\Z\right)^\times$。

设 $g=[1+\sqrt{k}]$ 是 $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times/\left(\Z/p\Z\right)^\times$ 的元素，对所有 $m\in\left\{1,\dots,p-1\right\}$ 有 $g^m=[1+m\sqrt{k}]=[m^{-1}+\sqrt{k}]$；还有 $g^p=[1]$。

设 $\alpha_m=\frac{m^{-1}+\sqrt{k}}{2}\in\mathcal{O}_k$。那么 $\alpha_m$ 是类 $g^m$ 的代表元。元素 $g^m$ 映射到 $\bar\varphi_p$ 的核的类 $[\varphi_p^{-1}(\alpha_m\mathcal{O}_k)]$。根据 [BTW95, 命题 2.9]，可以看到 $\alpha_m\mathcal{O}_k=\left(N(\alpha_m), -m^{-1} \bmod{2N(\alpha_m)} \right)$，其中余数从中心欧几里得除法计算。现在，
$$
\varphi_p^{-1}(\alpha_m\mathcal{O}_k)=\left(
N(\alpha_m), -pm^{-1} \bmod {2N(\alpha_m)}
\right).
$$
由于 $N(\alpha_m)=\frac{(m^{-1})^2-k}{4}$ 且 $q>4p$，可得 $p^2<N(\alpha_m)$，且 $-pm^{-1} \bmod {2N(\alpha_m)}=-pm^{-1}$。因此，该理想 $\varphi_p^{-1}(\alpha_m\mathcal{O}_k)$ 对应于二次型
$$
\left(
\frac{(m^{-1})^2-k}{4},  -pm^{-1},  p^2
\right),
$$
其判别式为 $kp^2$。此外，该型等价于型
$$
\left(
p^2,  pm^{-1}, \frac{(m^{-1})^2-k}{4}
\right),
$$
它对应于理想 $(p^2, m^{-1}p)$。

最终，$\mathcal{O}_{kp^2}$ 的该理想是约化的，因为 $\lvert m^{-1}p\rvert < p^2 < \sqrt{\lvert kp^2 \rvert}/2$。

因此，如果 $\mathfrak{t}=(p^2,p)$，那么 $[\mathfrak{t}]$ 生成 $\bar\varphi_p$ 的核，因为 $[\mathfrak{t}]=[\varphi_p^{-1}(\alpha_m\mathcal{O}_k)]$。此外 $[\mathfrak{t}]^m=[\varphi^{-1}(\alpha_m\mathcal{O}_k)]$，所以
$$
\mathtt{Reduce}\left( [\mathfrak{t}]^m \right) = 
\left(p^2, m^{-1}p\right)
$$
对每个 $m\in\left\{1,\dots,p-1\right\}$ 成立。     ■

## 问题

### Q1. 如何计算 $\bar\varphi_p, \varphi_p, \varphi_p^{-1}$？

(1) 根据文献 [CL09, 命题 2]，计算 $\bar\varphi_p$ 的方式如下：

输入是一个理想类 $\left[\mathfrak{a}\right] \in C(\mathcal{O}_{\Delta_p})$。算法搜索理想类中的元素 $\mathfrak{b}\in \left[\mathfrak{a}\right]$ 使得 $\mathfrak{b}$ 在 $\mathcal{O}_{\Delta_p}$ 中与 $p$ <mark>互质</mark>。搜索算法见于 [HJPT98, 算法 1]。进而，
$$
\bar\varphi_p\left(\left[\mathfrak{a}\right]\right)
= \left[\varphi_p(\mathfrak{b})\right]
= [\mathfrak{b}\mathcal{O}_{\Delta_K}].
$$
回忆一下，[CL09, 命题 1 之前的文字] $\mathcal{O}_{\Delta_p}$ 中的非零理想 $\mathfrak{a}$ 与整数 $p$ 互质，定义为
$$
\mathfrak{a}+f\mathcal{O}_{\Delta_p}=\mathcal{O}_{\Delta_p},
$$
[Cox99, 引理 7.18] 判定方式为判断 $\mathrm{N}(\mathfrak{a})$ 与 $p$ 是否互质。

(2) 根据文献 [CL09, 算法 2]，计算 $\varphi_p$ 的方式如下：

设输入理想为 $\mathfrak{a}_1=(a_1, b_1)$。注意，$\mathfrak{a}_1\in I(\mathcal{O}_{\Delta_p}, p)$。设输出理想为 $\mathfrak{a}_2=(a_2, b_2)$。注意，$\mathfrak{a}_2\in I(\mathcal{O}_{\Delta_K}, p)$。则

1. $a_2=a_1$。
2. 设 $\delta=\Delta_p\bmod2$ 为 $\Delta_p$ 的奇偶性。利用扩展欧几里得算法，求解 $u, v$ 使得 $uf+va_1\delta=1$。
3. $b_2=ub_1+va_1\delta~\mathrm{cmod}~2a_1$。

(3) 根据文献 [CL09, 算法 1]，计算 $\varphi_p^{-1}$ 的方式如下：

设输入理想为 $\mathfrak{a}_2=(a_2, b_2)$。注意，$\mathfrak{a}_2\in I(\mathcal{O}_{\Delta_K}, p)$。设输出理想为 $\mathfrak{a}_1=(a_1, b_1)$。注意，$\mathfrak{a}_1\in I(\mathcal{O}_{\Delta_p}, p)$。则

1. $a_1=a_2$。
2. $b_1=b_2p~\mathrm{cmod}~2a_1$。

### Q2. 什么是 $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times$？什么是 $\left(\mathbb{F}_p[X]/(X^2)\right)^\times$？为什么它们同构？

A2. 根据 GPT 4.1，$p\mathcal{O}_k$ 是 $\mathcal{O}_k$ 中由 $p$ 生成的理想。

$\mathcal{O}_{k}/p\mathcal{O}_k$ 是有限环：$\mathcal{O}_{k}$ 的元素模 $\mathcal{O}_k$ 中 $p$ 的所有倍数。更具体地说，任何元素都可以表示为 $a+b\omega$，其中 $a,b\in\Z_p$，且

$$
\omega=\begin{cases}
\sqrt{k},&k\equiv 2,3 \pmod 4,\\
\frac{1+\sqrt{k}}{2},&k\equiv 1 \pmod 4.
\end{cases}
$$


$\mathcal{O}_{k}/p\mathcal{O}_k$ 是环中的单位群（具有乘法逆元的元素）。

$\mathbb{F}_p[X]/(X^2)$ 是多项式环 $a+bX$（$a,b\in\mathbb{F}_p$）。单位是具有乘法逆元的元素。在该环中，$a+bX$ 可逆，当且仅当 $a\neq 0$。

### Q3. $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times/\left(\Z/p\Z\right)^\times$ 中元素的表示是什么？

A3. 为简单起见，取 $k\equiv 2,3\pmod 4$。即，取 $\mathcal{O}_k=\Z[\sqrt{k}]$。群 $\left(\mathcal{O}_{k}/p\mathcal{O}_k\right)^\times$ 由形式为 $a+b\sqrt{k}$ 的所有元素组成，其中 $a,b\in\Z_p$ 且 $a\neq 0$。群 $\left(\Z/p\Z\right)^\times$ 就是 $\Z_p^+$，由 1 到 $p$ 的整数组成。

商意味着我们将两个元素 $x,y$ 等同，如果它们相差 $\Z_p^+$ 中的因子。商的代表系为
$$
\begin{align}
[1]&=\set{1,2,\dots,p},\\
[a+\sqrt{k}]&=\set{\lambda a+\lambda\sqrt{k}\mid \lambda\in\Z_p^+}.
\end{align}
$$

* $[1]=\set{1,2,\dots,p}$，
* $[a+\sqrt{k}]$，

### Q4. 设 $g=[1+\sqrt{k}]$。如何计算 $g^m$？为什么 $[1+m\sqrt{k}]=[m^{-1}+\sqrt{k}]$？

根据 [Fraleigh8ed, 定理 12.7]，我们有 $[1+\sqrt{k}]^m=[(1+\sqrt{k})^m]$。接下来，我们有

$$
(1+\sqrt{k})^m\equiv1+m\sqrt{k}\pmod p.
$$
上述等式成立，因为 $p\mid k$，$\sqrt{k}$ 的幂生成 $k$ 因子，它模 $p$ 同余于 $0$。

### Q5. 判别式 $K=-pq$ 中参数 $p,q$ 的选择是否遵循什么道理？

在 `Gen` 算法中，我们首先构造基本判别式 $K=-pq$，使得 $C(K)$ 的 2-Sylow 子群的阶为 2。

根据 [Fraleigh8ed, 17.5 定义]，一个 $p$-Sylow 子群是 $p^n$ 阶子群，正整数 $n$ 是最大的。

文献 [CL15, 附录 B.3] 给出了解释。大意是，构造一个判别式 $K$，使得 $C(K)/\textrm{2-Sylow}$ 是循环群的概率尽可能大。

追问前提 - 为什么要追求这样的群结构？这是为了尽可能提高群元素 DL 问题的难度。有限交换群的 DL 难度大体上遵循如下顺序，降序排序：

1. 群阶为质数。例如 secp256k1 曲线群。
2. 群为循环群。
3. 群为交换群。

再概括一下 [CL15, 附录 B.3] 如何解释和提高 DL 在群 $C(K)/\textrm{2-Sylow}$ 上的难度。

根据 Cohen-Lenstra 假设 [Coh00, 猜想 5.10.1]，$C(K)/\textrm{2-Sylow}$ 是循环群的概率约为 97.76%。即使不是循环群，文献 [HM00] 指出，计算类数，或者（在不知道具体数的情况下）搜索类数的质因子，都是很困难的；因此无法使用 Pohlig-Hellman 算法。

文献 [HM00] 建议尽可能缩小类群的 2-Sylow 子群。文献 [Kap78, p. 598] 还指出，如果 $(p/q)=(q/p)=-1$，那么 $C(K)$ 的 2-Sylow 子群的阶就只有 2。

根据文献 [Coh00, 定理 4.9.15]，类数 $h(K)$ 的"平均长度"约为 $\sqrt{K}$。根据 [CL15, 附录 B.4]，$g$ 的阶约为 $\sqrt{K}$。

### Q6. 在 [CL15, 图 2] 中，什么叫设置 $\mathfrak{r}$ 为位于 $r$ 上的理想？

原文有这么一句话：然后，使用 [HJPT98, 3.1 小节] 的方法，我们构造 $\mathcal{O}_{\Delta_K}$ 的范数为 $r$ 的理想 $\mathfrak{r}$，其中 $r$ 是满足 $\left(\frac{\Delta_K}{r}\right)=1$ 的素数。查阅前述引用文献得知，$\mathfrak{r}=(r, b_r)$。理想的 a 系数为 $r$，就是在说 $\mathrm{N}(\mathfrak{a})=r$。

### Q7. 谁都能解 $f^m$ 的离散对数？

非最大整环相对于最大整环的介导系数为 $p$。该参数在 [DMZ21] 的源码中被实现为常量。这等价于把理想类 $f$ 实现为常量。这岂不是导致谁都能解 $f^m$ 的离散对数？

没错，$f^m$ 离散对数的确是谁都能解。这是因为 $f^m=[(p^2, m^{-1}p)]$，明文就是对第二个参数求模 p 乘法逆元。<mark>但是 $c_2 = h^r f^m$ 就没这么简单了</mark>。只有私钥持有者才能构造出 $c_1^{-x} = h^{-r}$，从而消掉 $c_2$ 中的 $h_r$。

总的来说，$c_1=g^r$ 给密文提供随机扰动。$h^r$ 提供加密。$f^m$ 映射明文。

### Q8. 明文映射群 $\left< f \right>$ 的阶为 $p$ 的理论依据是什么？

文献 [CL15, 附录 B.1] 末尾有提到，映射 $\bar\varphi_p$ 的核的阶可以用类数解析公式直接算出来，即 $h(\mathcal{O}_{\Delta_p})/h(\mathcal{O}_{\Delta_K})=p$。前述映射的核就是明文映射群 $\left< f \right>$。

### Q9. 为什么 $h^{-r}$ 的离散对数是困难的？

文献 [CL15, 附录 B.3] 的解释是，计算 DL 的难度与计算类数相当，在判别式很大时难以计算类数，因此难以计算 DL。但这个解释不太能说服我。由于 $\left< f \right>$ 的出现，我总觉得必须要回答以下问题：

> 设 $\left<g\right>$ 为整数环 $\mathcal{O}_{\Delta_K}$ 中最大的循环群。设 $g^x=[(a, b)]$。如何论证 $a, b, \Delta_K$ 解不出 $x$？

回答该问题需要阅读 [CL15, 附录 B.3] 的上游文献。即使不回答该问题，目前掌握的知识足以移植 DMZ 代码。可以先相信该文献以及上游文献的信誉。

### Q10. 如何构造类群的生成元 $g$？

Step 1. 搜索质数 $p, q$，满足

* $\log q \ge 2\log p$。
* $(p/q)=(q/p)=-1$。

Step 2. 搜索小质数 $r\neq p$ 使得 $(\Delta_K/r)=1$。Kronecker 符号已有实现。

```
https://docs.rs/rug/latest/rug/struct.Integer.html#method.kronecker
```

Step 3. 求解 $b_r$ 使得 $b_r^2\equiv \Delta_K \pmod{4r}$。定义 $O_{\Delta_K}$ 的理想 $\mathfrak{r}=(r,b_r)$。求解二次余子需要 Tonelli-Shanks 算法，已有如下 Python 实现。这一步依据文献 [HJPT98, 3.1 小节]。

```
https://github.com/GiacomoPope/ClassGroups/blob/d3020c3b7f7af9b6588ce2176b8b7c6cfbb18602/classgroup_helper.py#L40
```

Step 4. 计算如下公式。其中函数 $\varphi_p^{-1}$ 详见本文 Q1；$k$ 是随机数 $1\sim p$。
$$
g=\left[ \varphi_p^{-1}(\mathfrak{r}^2)\right]^{p}f^k.
$$
Step 5. （可选）估计

### Q11. 更快的变体？

[CL15, 4.2 节] 开头提到的 $\psi$ 怎么计算？

重新定义生成元 $g=\left[\mathfrak{r}^2\right]$。如此，$g$ 是 $O_{\Delta_K}$ 的理想类，而不再是 $O_{\Delta_p}$ 的理想类。重新定义公钥 $h=g^x$。定义映射
$$
\psi: C(O_{\Delta_K})\rightarrow C(O_{\Delta_p}),\quad 
\mathfrak{a}\mapsto
\left[ \varphi_p^{-1}(\mathfrak{a})\right]^{p}.
$$
重新定义加密算法。
$$
\begin{align}
c_1 &= g^r,\\
c_2 &= f^m \psi (h^r).
\end{align}
$$
重新定义解密算法。
$$
\begin{align}
c_1' &= \psi(c_1^x), \\
f^m  &= c_2\cdot c_1^{-1}.
\end{align}
$$
